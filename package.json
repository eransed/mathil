{
  "name": "mathil",
  "version": "0.12.0",
  "author": "eransed",
  "homepage": "https://github.com/eransed/mathil",
  "description": "Toy math library",
  "keywords": [
    "math",
    "vector",
    "matrix",
    "geometry",
    "2D",
    "3D",
    "canvas",
    "utils"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "npm i && npm ls && npx -p typescript tsc --version && npx -p typescript tsc",
    "test": "npm run build && node ./dist_test/tests/test.js",
    "publish": "npm run build && npm test && npm publish"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}