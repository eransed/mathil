{
  "name": "mathil",
  "version": "0.12.0",
  "author": "eransed",
  "homepage": "https://github.com/eransed/mathil",
  "description": "Toy math library",
  "keywords": [
    "math",
    "vector",
    "matrix",
    "geometry",
    "2D",
    "3D",
    "canvas",
    "utils"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "prebuild": "npm i && npm ls && cd src && node ./_prebuild.mjs",
    "build": "npm run prebuild && npx -p typescript tsc --version && npx -p typescript tsc",
    "test": "npm run prebuild && cd tests && npx -p typescript tsc && node ../test_build/tests/test.js",
    "publish": "npm run build && npm test && npm publish"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}